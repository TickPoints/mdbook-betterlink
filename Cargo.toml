[package]
name = "mdbook-betterlink"
version = "0.1.0"
authors = ["TickPoints <tickpoints@outlook.com>"]
description = "Plugin that provides better hyperlink effects for mdbook."
edition = "2024"
license = "MIT"

[dependencies]
# core
mdbook = {version = "0.4", default-features = false}
serde_json = "1.0"

# command
clap = "4.5"

# logger
log = "0.4"
env_logger = "0.11"
